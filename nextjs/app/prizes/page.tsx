import { StubPage } from "~~/components/StubPage";

export default function PrizesPage() {
  return (
    <StubPage
      route="prizes"
      blurb="Draw history and prize claims. Winner identities are revealed only through KMS-verified disclosure; amounts stay encrypted until claimed. Ships next."
    />
  );
}
