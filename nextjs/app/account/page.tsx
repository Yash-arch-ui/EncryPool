import { StubPage } from "~~/components/StubPage";

export default function AccountPage() {
  return (
    <StubPage
      route="account"
      blurb="Your position, decrypted for your eyes only — balance, checkpoints and history via Zama user decryption. Ships next."
    />
  );
}
